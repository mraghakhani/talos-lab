# Copy to config/lab.local.env for machine-local overrides.
# config/lab.local.env is gitignored.

# The DNS provider will be needed later for cert-manager DNS-01.
# DNS_PROVIDER=cloudflare

# Optional explicit proxy override.
# PROXY_HOST=192.168.231.1
# PROXY_PORT=10808
